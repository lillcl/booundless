{
  "name": "kc-carai",
  "version": "0.1.0",
  "private": true,
  "description": "無界啟程 BOOUNDLESS vehicle and maintenance web app. Vanilla HTML/CSS/JS frontend, serverless API, Postgres persistence.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "start": "node scripts/dev-server.js",
    "seed": "node db/seed.js",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/*.test.js",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "jose": "^5.9.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
