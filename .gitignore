# Node / JS
node_modules/
.next/
out/
dist/
build/
.cache/
.parcel-cache/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env / Secrets
.env
.env.*
!.env.example

# Local SQLite database (per-developer file; seed data lives in db/seed.js)
db/*.db
db/*.db-journal
db/*.db-wal
db/*.db-shm

# Reference assets (keep README + HTML, ignore bulky image binaries)
reference/kangcheng_v10_4/assets/

# Local secrets directory — never commit real keys
keys/*
!keys/README.txt