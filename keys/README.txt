# Real secrets go here. Each file is one key.
# Files in this directory are gitignored.

# Example — drop your actual key into a file matching the env var name.
# Example: if MAPS_API_KEY=abc123, create ./keys/MAPS_API_KEY with content:
# abc123